---
image: archlinux
packages:
  - python
  - uv
sources:
  - https://git.sr.ht/~jmcantrell/swaystatus
tasks:
  - setup: |
      cd swaystatus
      uv sync --frozen
  - test: |
      cd swaystatus
      uv run --frozen ./scripts/test
